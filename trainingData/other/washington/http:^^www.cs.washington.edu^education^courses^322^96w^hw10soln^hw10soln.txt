date wed jan gmt server ncsa content type text html cse assignment solution set friday march next document cse assignment solution set friday march thanks p kromann trick one dpda given language computation abbac function consists rules turing machine accepts brief description states read first blank start match loop looks b sees c re done matching first string ve read b re looking c ve read re looking c re trying match b re trying match scan left c scan left next thing match first string verify input second string ve matched everything first string found nothing else second string accept input computation input abcab let assume productions p form numbered m pda accept language generated g need specify q function consists following rules document james fix tue mar pst