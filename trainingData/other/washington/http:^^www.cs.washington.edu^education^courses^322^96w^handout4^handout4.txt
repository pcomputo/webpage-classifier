date wed jan gmt server ncsa content type text html halting problem next document halting problem halting problem defined input turing machine m binary input x property m halts input x theorem halting problem undecidable proof proof contradiction suppose halting problem decidable must turing machine h property h halts inputs accepts m halts input x recall binary representation turing machine m define new turing machine d following inputs form copy input form string run h input h halts without accepting d halts h halts accepts d simply loops forever two possibilities d halts input d halt input come contradiction cases d halts input definition h h accepts definition d d halt input d halt input definition h h halts input without accepting definition d d halts input document james fix tue mar pst