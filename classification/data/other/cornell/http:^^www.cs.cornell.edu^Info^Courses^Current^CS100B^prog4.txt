mime version server cern date sunday nov gmt content type text html content length last modified friday nov gmt cs b program solution cs b program solution decided implement bigint define digits typedef struct unsigned short array digits int digits bigint keep least significant digit index significant digit digits implementation bigint library sterling approximation code many programs saw used two loops determine n inefficient need one loop